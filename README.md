# PRSentinel
Automated PR review agent built with Django + Celery that uses an LLM to flag bugs, security issues, and style violations on every GitHub pull request — posts inline comments in real time via webhooks, with confidence-based filtering to cut down false positives.
